Question 23: You are working on a Content-based application based on a flutter, client wants you to show a random lucky number on the click button. Which is the max limit is 350. What things will you require to achieve this Problem statement?

Hint 1:- Add the method for generating random lucky numbers.
Hint 2:- Add text and an Elevated button widget.
code:
import 'dart:math'; // For generating random numbers
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: RandomLuckyNumberScreen(),
    );
  }
}

class RandomLuckyNumberScreen extends StatefulWidget {
  @override
  _RandomLuckyNumberScreenState createState() => _RandomLuckyNumberScreenState();
}

class _RandomLuckyNumberScreenState extends State<RandomLuckyNumberScreen> {
  // Declare a variable to store the lucky number
  int luckyNumber = 0;

  // Method to generate a random number between 1 and 350
  void generateLuckyNumber() {
    setState(() {
      luckyNumber = Random().nextInt(350) + 1; // Random number from 1 to 350
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lucky Number Generator'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Display the lucky number
            Text(
              'Your Lucky Number: $luckyNumber',
              style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 20),
            // Button to generate a new lucky number
            ElevatedButton(
              onPressed: generateLuckyNumber, // Trigger the random number generation
              child: Text('Generate Lucky Number'),
            ),
          ],
        ),
      ),
    );
  }
}
