Question 06: Build a Flutter widget that displays two text elements in two separate columns. The first text element should say "First Name" and the second should say "Last Name". Make sure the two text elements are evenly spaced and centered within their respective columns. 
Code:
import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Two Text Columns'),
        ),
        body: Center(
          child: TwoTextColumns(),
        ),
      ),
    );
  }
}

class TwoTextColumns extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.center, // Center the row
      children: [
        // First Column: "First Name"
        Expanded(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center, // Center text in the column
            children: [
              Text(
                'First Name',
                style: TextStyle(fontSize: 24),
              ),
            ],
          ),
        ),
        
        // Spacer between the two columns (optional)
        SizedBox(width: 20),
        
        // Second Column: "Last Name"
        Expanded(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center, // Center text in the column
            children: [
              Text(
                'Last Name',
                style: TextStyle(fontSize: 24),
              ),
            ],
          ),
        ),
      ],
    );
  }
}
